/* 

-Bug: playerPromise is calling after every pressing of select player btn , that's why the data is fetching everytime
-Fix: Move the playerPromise variable into a useState inside -App.tsx

-Deploy the dist into Netlify.app



*/